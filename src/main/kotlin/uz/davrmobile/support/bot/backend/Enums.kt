package uz.davrmobile.support.bot.backend

enum class ErrorCode(val code: Int) {
    USER_NOT_FOUND(640),
    SOMETHING_WENT_WRONG(641),
    USER_ALREADY_EXISTS(642),
    SESSION_NOT_FOUND(643),
    SESSION_ALREADY_BUSY(644),
    SESSION_CLOSED(645),
    MESSAGE_NOT_FOUND(646),
    UN_SUPPORTED_MESSAGE_TYPE(647),
    NO_SESSION_IN_QUEUE(648),
    BOT_NOT_FOUND(649),
    BAD_CREDENTIALS(650),
    ACCESS_DENIED(651),
    USERNAME_ALREADY_EXISTS(652),
    BUSY_SESSION(653),
    FILE_NOT_FOUND(654),
    OPERATOR_LANGUAGE_NOT_FOUND(655),
    STANDARD_ANSWER_NOT_FOUND(656),
    STANDARD_ANSWER_ALREADY_EXISTS(657),
    BOT_ALREADY_STOPPED(658),
    BOT_ALREADY_ACTIVE(659),
    NO_AUTHORITY(660),
    SESSION_NOT_CONNECTED_TO_OPERATOR(661),
    TEXT_CANT_BE_EMPTY(662),
    MAXIMUM_TEXT_LENGTH(663),
    BOT_TOKEN_NOT_VALID(664),
    INFORMATION_NOT_FOUND(665),
    FIELD_IS_REQUIRED(666),
    FIELD_CANT_BE_EMPTY(667),
    BOT_IS_NOT_RUNNING(668),
    FIELD_NOT_FOUND_SUGGESTION(669)
}

enum class UserRole {
    ADMIN,
    USER,
    OPERATOR
}

enum class LanguageEnum {
    UZ,
    RU,
    EN
}

enum class BotMessageType {
    TEXT,
    PHOTO,
    VIDEO,
    VIDEO_NOTE,
    LOCATION,
    CONTACT,
    DICE,
    STICKER,
    AUDIO,
    ANIMATION,
    DOCUMENT,
    VOICE,
    POLL
}

enum class UserStateEnum {
    NEW_USER,
    SEND_PHONE_NUMBER,
    SEND_FULL_NAME,
    CHOOSE_LANG,
    ACTIVE_USER,
    ASK_YOUR_QUESTION,
    TALKING,
    WAITING_OPERATOR
}

enum class SessionStatusEnum {
    WAITING,
    BUSY,
    CLOSED
}

enum class BotStatusEnum {
    STOPPED,
    ACTIVE,
}

enum class MessageEffects(val id: String) {
    FIRE("5104841245755180586"),
    LIKE("5107584321108051014"),
    DIS_LIKE("5104858069142078462"),
    HEART("5044134455711629726"),
    CONFETTI("5046509860389126442")
}