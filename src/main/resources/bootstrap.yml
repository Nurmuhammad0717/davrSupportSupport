spring:
  application:
    name: support-service

---
spring:
  profiles: prod
  cloud:
    config:
      uri: http://config-service:8888

---
spring:
  profiles: local
  cloud:
    config:
      uri: http://localhost:8888