server:
  port: 80

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/davr_support_test
    username: postgres
    password: Sobirov0717
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true